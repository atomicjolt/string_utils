String functions from https://gobyexample.com/collection-functions

# Build:
go build

# Run all Tests:
gotest -v

# Build lib:
  `go mod tidy`             // Clean up the mod file
  `go build`
  `git push`
